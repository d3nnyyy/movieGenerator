version: 0.2

phases:
  build:
    commands:
      - echo Entering the build phase...
      - mvn clean package
  post_build:
    commands:
      - echo Build completed successfully!
artifacts:
  files:
    - target/movie-generator-backend.jar
  discard-paths: yes